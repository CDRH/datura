**/config.xsl
# ignore only config and private yml files
**/config.yml
**/private.yml

# ignore all the repositories in the collections directory
collections/*
# except the example directory
!collections/example

# ignore tmp directory even though will need to create it each time
tmp/*

# ignore logs because there are so many of them!
logs/*
!logs/.keep

**/.DS_Store
